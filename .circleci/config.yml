version: 2.1

orbs:
    azure-acr: circleci/azure-acr@0.2.0
    docker: circleci/docker@2.1.2
    azure-aks: circleci/azure-aks@0.3.0
    azure-cli: circleci/azure-cli@1.2.2
    helm: circleci/helm@2.0.0

executors:
  posts-executor:
    environment:
      CONTAINER_IMAGE_NAME: posts/posts
    docker:
      - image: cimg/openjdk:8.0

jobs:
  build:
    docker:
      - image: maven:3.8.1-jdk-11
      - image: circleci/mysql

    steps:
      - checkout
      - restore_cache:
          key: jars-{{ checksum "pom.xml" }}

      - run: mvn dependency:go-offline

      - save_cache:
          paths:
            - ~/.m2
          key: jars-{{ checksum "pom.xml" }}

      - attach_workspace:
          at: .

      # Build and test the project
      - run:
          name: Build
          command: mvn clean install

      - persist_to_workspace:
          root: .
          paths:
            - target/build

      - store_artifacts:
          path: ./target/build
  test:
    docker:
      - image: maven:3.8.1-jdk-11
      - image: circleci/mysql

    steps:
      - checkout
      - restore_cache:
          key: jars-{{ checksum "pom.xml" }}
      - run:
          name: Test
          command: mvn test
      - save_cache:
          paths:
            - ~/.m2
          key: jars-{{ checksum "pom.xml" }}

      - store_artifacts:
          path: target/surefire-reports
      - store_test_results:
          path: target/surefire-reports

  deploy:
    docker:
      - image: cimg/base:stable

    steps:
      - checkout

      - attach_workspace:
          at: .

      - add_ssh_keys:
          fingerprints:
            - "SHA256:bdQhUpWvsTenmxgrI/Z4u49QPvaTPhHqZk0SZpzUuQE"
      - run:
          name: Install SSH Client
          command: sudo apt-get update && sudo apt-get install -y openssh-client

      - run:
          name: Verify build artifact
          command: ls -l target/build/docker-post-app.war

      - run:
          name: SSH connection and deployment
          command: |
            scp -v -o StrictHostKeyChecking=no target/build/docker-post-app.war $SSH_USER@$SSH_HOST:/opt/tomcat/webapps/ROOT.war           
          no_output_timeout: 20m

        # Add SSH server fingerprint to known hosts
     # - run:
      #   name: Add SSH Host
      #   command: ssh-keyscan -H $SSH_HOST >> ~/.ssh/known_hosts
          #- run:
          #name: "echo an env var that is part of our project $SSH_PRIVATE_KEY"
            #command: |
          #echo $SSH_PRIVATE_KEY # this env var must be set within the project

          #      - run:
          #name: "echo an env var that is part of our project $SSH_USER"
            #command: |
          #echo $SSH_USER # this env var must be set within the project

          #- run:
          #name: "echo an env var that is part of our project $SSH_HOST"
            #command: |
          #echo $SSH_HOST # this env var must be set within the project

      #- run:
       #   name: Copy .war File
        #  command: scp target/build/docker-post-app.war $SSH_USER@$SSH_HOST:/opt/tomcat/webapps/ROOT.war

      #- run:
       #   name: Copy .war to Linux server
        #  command: |
         #   # Install ssh client
          #  sudo apt-get update && sudo apt-get install -y ssh

            # Copy the .war file to the Linux server via SSH
#            scp -i ~/.ssh/id_rsa target/build/docker-post-app.war $SSH_USER@$SSH_HOST:/opt/tomcat/webapps/ROOT.war
workflows:
  version: 2
  build_test_deploy:
    jobs:
      - build
      - test
      - deploy:
          requires:
            - build
            - test